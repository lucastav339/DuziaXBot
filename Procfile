# Use APENAS UMA das linhas abaixo (comente a outra).

# 1) Worker (Polling) — simples, não abre porta:
worker: python main.py

# 2) Web (Webhook) — precisa de URL pública e PORT:
# web: python main.py
